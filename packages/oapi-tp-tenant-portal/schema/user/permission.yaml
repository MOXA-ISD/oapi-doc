swagger: '2.0'

info:
  version: 0.0.2
  title: ThingsPro Tenant Portal RESTful API reference
basePath: '/api/v1'
schemes:
  - http
  - https
produces:
  - application/json

tags:
  - name: permission
    description: Handles permission field data

paths:
  '/permissions':
    get:
      description: Get all permission field data
      tags:
        - permission
      responses:
        '200':
          description: success
          schema:
            type: array
            items:
              $ref: '#/definitions/Permission'
          examples: { 'application/json': { $ref: '#/externalDocs/x-mocks/PermissionList' } }

  '/permissions/{id}':
    parameters:
      - name: id
        in: path
        description: Permission ID
        required: true
        type: integer
    get:
      description: Get permission field data by id
      tags:
        - permission
      responses:
        '200':
          description: success
          schema:
            type: array
            items:
              $ref: '#/definitions/Permission'
          examples: { 'application/json': { $ref: '#/externalDocs/x-mocks/Permission' } }

  '/permissions/type/{type}':
    parameters:
      - name: type
        in: path
        description: Permission type - 1=tenant, 2=group
        required: true
        type: integer
    get:
      description: Get permission field data by type
      tags:
        - permission
      responses:
        '200':
          description: success
          schema:
            type: array
            items:
              $ref: '#/definitions/Permission'
          examples: { 'application/json': { $ref: '#/externalDocs/x-mocks/PermissionList' } }

definitions:
  Permission:
    title: Permission
    type: object
    properties:
      id:
        type: integer
        description: Unique identifier representing a specific user.
      name:
        type: string
        description: Name of permission.
      type:
        type: integer
        description: Naming of permission group type
        enum: [1, 2]
      description:
        type: string
        description: Description of permission.

externalDocs:
  url: 'http://#'
  x-mocks:
    Permission:
      {
        "id": 1,
        "name": "User/Role management",
        "type": 1,
        "description": "User/Role management"
      }
    PermissionList:
      {
        "data": [
          {
            "id": 1,
            "name": "User/Role management",
            "type": 1,
            "description": "User/Role management"
          },
          {
            "id": 2,
            "name": "Device register/grouping",
            "type": 1,
            "description": "Device register/grouping"
          },
          {
            "id": 3,
            "name": "View device information",
            "type": 2,
            "description": "View device information"
          },
          {
            "id": 4,
            "name": "Full manage device",
            "type": 2,
            "description": "Full manage device"
          }
        ]
      }
      